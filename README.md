# Clean Room Systems

Профессиональный сайт-каталог оборудования и расходных материалов для чистых помещений.

## Технологии

- **Frontend:** Next.js 14, React 18, TypeScript
- **Стили:** Tailwind CSS
- **База данных:** SQLite (Prisma ORM)
- **Аутентификация:** NextAuth.js
- **Иконки:** Lucide React

## Установка и запуск

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка базы данных

```bash
# Создание таблиц в базе данных
npx prisma db push

# Заполнение демо-данными
npm run db:seed
```

### 3. Запуск в режиме разработки

```bash
npm run dev
```

Сайт будет доступен по адресу: http://localhost:3000

### 4. Сборка для продакшена

```bash
npm run build
npm run start
```

## Структура проекта

```
src/
├── app/                    # Страницы приложения (App Router)
│   ├── admin/             # Админ-панель
│   ├── api/               # API endpoints
│   ├── catalog/           # Каталог товаров
│   ├── company/           # Страницы о компании
│   ├── contacts/          # Страница контактов
│   ├── delivery/          # Доставка и оплата
│   ├── news/              # Новости и статьи
│   └── privacy/           # Политика конфиденциальности
├── components/            # React компоненты
│   ├── admin/             # Компоненты админ-панели
│   ├── forms/             # Формы
│   ├── layout/            # Header, Footer
│   ├── providers/         # Context провайдеры
│   ├── sections/          # Секции страниц
│   └── ui/                # UI компоненты
├── config/                # Конфигурация
├── lib/                   # Утилиты и хелперы
└── types/                 # TypeScript типы
```

## Админ-панель

Доступ к админ-панели: http://localhost:3000/admin

**Демо-доступ:**
- Email: admin@cleanroom.uz
- Пароль: admin123

### Функции админ-панели:

- Управление товарами и категориями
- Просмотр и обработка заявок с сайта
- Редактирование новостей и статей
- Управление партнерами и сертификатами
- Настройки слайдера на главной странице
- Редактирование статических страниц

## SEO

Сайт оптимизирован для поисковых систем:

- Семантическая HTML-разметка
- Meta-теги для каждой страницы
- Open Graph разметка для соцсетей
- JSON-LD структурированные данные (Schema.org)
- Автоматическая генерация sitemap.xml
- Файл robots.txt

## Адаптивность

Сайт полностью адаптивен и корректно отображается на:

- Десктопах (1920px+)
- Ноутбуках (1024px - 1919px)
- Планшетах (768px - 1023px)
- Мобильных устройствах (до 767px)

## Переменные окружения

Создайте файл `.env` в корне проекта:

```env
DATABASE_URL="file:./dev.db"
NEXTAUTH_SECRET="your-super-secret-key"
NEXTAUTH_URL="http://localhost:3000"
```

## Лицензия

MIT
